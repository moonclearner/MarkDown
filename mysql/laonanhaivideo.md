# video need to be learning

## repr
格式化函数

## 程序排错
了解程序到底在干嘛（strac）
ps -ef |grep whoisyouwant
strace -p whoisport

即可显示该端口的运行情况

## dd命令
linux bash  dd if=/dev/sda of=test.dd  产生一个test.dd 文件里面是有数据的

## md5sum filename  
返回一个为一个md5值，用于检验文件是否唯一
